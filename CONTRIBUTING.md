# איך לתרום

תודה שאתם כאן. הכלי הזה חי מביקורת טובה, כמה מהשיפורים המרכזיים בו
(מדד הנבחרוּת, תוספת הרצינות, תיקון פיזור הציונים) הגיעו ממשוב ציבורי.

## דרכי תרומה

1. **Issue** - הדרך המהירה. מתאים ל: טעות בפרופיל מועמד/ת, באג, רעיון
   למדד או לשאלה בשאלון, ביקורת סטטיסטית על המודל.
2. **Pull Request** - עשו fork, עבדו על branch, פתחו PR. כללים:
   - PR אחד = נושא אחד. PRים קטנים וממוקדים נסקרים מהר.
   - שינוי במנוע הדירוג חייב לעבור את בדיקת ההטיה:
     `cd app && npx tsx scripts/simulate.ts` - יחסי החשיפה של קבוצות
     בתת-קבוצת הפרסונות הניטרליות חייבים להישאר בטווח [0.75, 1.35].
   - שינוי בנתוני מועמד/ת חייב מקור פומבי מצורף (קישור).
   - `npx tsc --noEmit && npm run build` ירוקים לפני הגשה.
   - בלי em-dashes בטקסטים.

## מה קורה עם ההצעה שלכם

מיזוג נעשה אך ורק על ידי בעל המאגר (@razkaplan). זה כלי שמשפיע על
הצבעה אמיתית, ולכן שרשרת האחריות קצרה: אדם אחד אחראי לכל שורה שנכנסת.
PR שלא מוזג יקבל הסבר למה.

## כללי התנהגות

הכלי עוסק בפוליטיקה; הדיונים כאן לא. Issues או PRs שמקדמים או תוקפים
מועמד/ת ספציפיים (להבדיל מתיקון עובדתי מגובה מקור) ייסגרו.

## עקרונות המודל (לפני שמציעים שינוי מהותי)

קראו את [docs/MODEL.md](docs/MODEL.md) ואת
[docs/political-science-review.md](docs/political-science-review.md).
עקרונות שלא נתפשר עליהם:
- שקיפות מלאה: כל נוסחה מוסברת בתוך האפליקציה.
- אין העדפה מערכתית לאף מועמד/ת; העדפות מגיעות רק מתשובות המשתמש/ת.
- כל מספר מגיע ממקור פומבי מדיד, לעולם לא מהערכה.
- הטיה מבנית נמדדת (סימולציית פרסונות) לפני כל שחרור.
